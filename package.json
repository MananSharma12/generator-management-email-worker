{
  "name": "generator-management-email-worker",
  "version": "1.0.0",
  "description": "Creating a worker to handle Sendgrid mails due to CORS limitation",
  "main": "index.js",
  "dependencies": {
    "@sendgrid/mail": "^8.1.3",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "author": "Manan Sharma",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "@vercel/node": "^3.2.12",
    "typescript": "^5.5.4"
  }
}
